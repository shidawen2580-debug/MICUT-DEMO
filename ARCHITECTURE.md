# Architecture

## 架构目标

这个展示版仓库采用 harness 风格的分层组织思路：让业务核心、应用编排、外部适配和 Electron 运行壳彼此解耦，避免把真实业务能力绑定死在某一个入口层里。

## 分层说明

### `domain/`

放业务对象、规则、仓储接口和任务语义。

这里不依赖 Electron、SQLite 或 FFmpeg 等外部实现，重点是表达“系统应该如何理解项目、打点、规则和剪辑任务”。

### `app/`

放应用用例与编排逻辑。

这一层负责把多个领域对象和仓储接口组织成可执行动作，比如创建项目、绑定视频、导入打点、更新剪辑规则、发起任务等。

### `adapters/`

放外部能力接入：

- SQLite 持久化
- Excel 导入
- FFmpeg 剪辑
- 文件系统与本地存储路径
- 视频探测

这层的作用是把“外部世界”接进来，同时不污染领域层。

### `main/` + `preload/` + `renderer/`

这是 Electron 运行壳：

- `main/` 负责主进程启动、组合根和 IPC 注册
- `preload/` 负责桥接受控能力
- `renderer/` 负责最小交互界面

## 为什么说它是 harness 风格

这里的 harness，不是指一个单独的框架名，而是指一种组织方式：

- 用清晰边界承载真实业务
- 用可替换适配层隔离外部依赖
- 用受控入口把产品流串起来
- 用测试和 smoke 脚本守住最小可运行闭环

这类结构很适合 vibecoding 迭代：先把真实流程跑通，再逐步增强每一层，而不是一开始就把实现锁死在混杂的运行壳里。
